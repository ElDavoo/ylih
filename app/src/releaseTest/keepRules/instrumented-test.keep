# Keep rules for the *app* APK the instrumented tests install — the releaseTest build type only.
# Nothing here reaches `release`: src/releaseTest/keepRules is read by the R8 run for that build
# type alone, the same trick releaseTestImplementation(ui-test-manifest) uses one file over.
#
# Why any of this is needed. AGP builds the androidTest APK with `-keep class * {*;}` plus
# `-applymapping` against the app's mapping file, and it packages nothing the app already
# provides — no Kotlin runtime, no app classes, not even androidx.tracing. So every symbol the
# test code links against has to still exist in the app APK under the name the mapping gave it.
# A class R8 *removed* from the app has no mapping entry, so the reference keeps its original
# name and resolves to nothing: NoClassDefFoundError on a device, invisible to every unit test
# and to the mapping-file check. That is not a hypothetical — it is what
# `connectedClassicReleaseTestAndroidTest` failed with before this file existed.
#
# The rules are grouped by what forced them, because that is what tells a later reader whether
# one can go away.

# 1. The instrumentation runner. AndroidJUnitRunner.onCreate calls androidx.tracing.Trace, which
#    arrives as a transitive dependency of both the app and androidx.test and so is packaged in
#    neither APK once the app stops using it. The crash is at process start, before a single test
#    runs — "Starting 0 tests", then "Instrumentation run failed due to Process crashed".
-keep class androidx.tracing.Trace { *; }

# 2. Kotlin's runtime. The test source set is Kotlin, so its compiled code calls into the stdlib
#    and the coroutines runtime for things it never mentions: `runBlocking` is BuildersKt, every
#    suspend call is IntrinsicsKt/Boxing/SpillingKt/DebugProbesKt, `use` is CloseableKt, `by lazy`
#    is LazyKt. R8 inlines most of those facades away in the app, since the app's own call sites
#    are all it can see. Kept wholesale rather than enumerated: the list is a by-product of
#    whatever the tests happen to compile to, so an enumeration would drift into this same crash
#    the next time a test gains a `use` or a `withTimeout`. These are library packages — app code
#    under it.eldavo.ylih is still shrunk, renamed and merged exactly as it is for release, which
#    is the part the tests are here to check.
-keep class kotlin.** { *; }
-keep class kotlinx.coroutines.** { *; }
# androidx.test:core reaches its Kotlin helpers through these; same story as above.
#
# One reference this cannot satisfy: androidx.test:core asks for concurrent-futures-ktx 1.2.0,
# AGP pins the test classpath to whatever the app resolved — 1.1.0, via work-runtime-ktx — and
# SuspendToFutureAdapter only exists from 1.2.0, so the class is in no version on the classpath
# and no keep rule can hold it. Only ViewCapture and WindowCapture touch it, neither of which
# this suite calls, so the reference is dormant. Raising the app's own dependency to 1.2.0 for
# the sake of a screenshot helper the tests never use is the more expensive of the two mistakes.
-keep class androidx.concurrent.futures.** { *; }

# 3. The app API the tests call. `-applymapping` renames the test APK's references; it cannot
#    reshape them, so any *optimisation* R8 applies to a method the tests call breaks the call
#    site even when the class survives. Both halves of that were observed here:
#      - deleted outright — Clock is a fun interface the app only ever hands Clock.Wall, the DAO
#        interfaces are each implemented once and get merged into their _Impl, and
#        YlihDatabase.Companion holds a single call and is inlined into it. The mapping file says
#        so in as many words: YlihDatabase$Companion -> R8$$REMOVED$$CLASS$$508.
#      - reshaped — JsonBackup.export survived and was staticised, so the test APK's
#        invoke-virtual found nothing: "No virtual method a(YlihDatabase, long, Continuation)".
#
#    allowobfuscation rather than a plain -keep, deliberately. Renaming is the part of R8 these
#    tests exist to check — it is what breaks a lookup that goes through a string — so the tested
#    build still renames these classes and the mapping still has to carry the test APK onto them.
#    What is given up is shrinking and optimisation across two packages of the data layer, which
#    is not what any test in the suite asserts on: Room's generated _Impl still has to survive
#    under its own name (Room's consumer keep rules, not this file, are what hold it), the worker
#    still has to be reachable by class name, the serializers still have to carry their generated
#    descriptors, and Compose still has to start.
-keep,allowobfuscation class it.eldavo.ylih.data.** { *; }
-keep,allowobfuscation class it.eldavo.ylih.export.** { *; }

# 4. androidx.glance.appwidget.GlanceAppWidget — the return type of the one call the widget test
#    makes, `(receiver as GlanceAppWidgetReceiver).glanceAppWidget`. This is the removal case
#    above rather than the reshaping one, and it arrived without a line of it.eldavo.ylih.widget
#    changing name: giving the three widgets a common base class left GlanceAppWidget with
#    exactly one direct subclass, which is all R8 needs to merge a class vertically into it. The
#    shipped APK is perfectly consistent afterwards — nothing reaches the base class by name, and
#    the widgets Glance does look up by name are held to theirs by main's glance-widgets.keep.
#    But a class R8 merged away has no mapping entry, and a dex method reference carries its
#    return type, so the test APK's call site and the app's method stop being the same method:
#      test APK: invoke-virtual Ldh0;.a:()Landroidx/glance/appwidget/GlanceAppWidget;
#      app APK:  dh0.a -> ()Lhw2;    where hw2 is it.eldavo.ylih.widget.YlihWidget
#    NoSuchMethodError on the device, and invisible to every unit test and to the mapping check,
#    which reads the names that survived rather than the ones that no longer resolve. Keeping the
#    class blocks the merge for this build type alone; release still merges it away.
-keep,allowobfuscation class androidx.glance.appwidget.GlanceAppWidget
