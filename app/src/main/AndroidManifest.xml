<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Receiving ACL_CONNECTED/ACL_DISCONNECTED and reading a headset's name and class. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Only used in detailed-tracking mode; wired plug events need a running process.
         The classic flavor adds FOREGROUND_SERVICE_SPECIAL_USE on top of this. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />

    <!--
      hasFragileUserData puts a "keep app data" checkbox in the system's uninstall dialog (API 29+,
      hence the targetApi — minSdk is 26 and lint would otherwise call the attribute unused). It is
      the one way history survives a deliberate uninstall and reinstall on the same phone: cloud
      backup covers a new phone and only where the user has it enabled, and the JSON export is a
      thing someone has to remember to do first.
    -->
    <application
        android:name=".YlihApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ylih"
        tools:targetApi="29">

        <!--
          singleTop so a second tap on a widget row re-navigates the activity that is already up
          (see onNewIntent) instead of stacking another copy of the app behind it.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Ylih">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".tracking.TrackingService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!--
          What an on-device agent may ask this app, and how much of it. Nothing here differs
          between the classic and play builds, so it stays in the main manifest rather than
          becoming a fourth thing `Distribution` has to know about.

          The class is written by KSP from agent/YlihAppFunctions.kt and reached only by this
          name, which is what app-functions.keep exists for. Exported and bound with
          BIND_APP_FUNCTION_SERVICE because the caller is the system, and enabled through the
          library's own boolean: it extends a class that only exists on Android 17, so on anything
          older the resource resolves to false and the service is simply absent rather than bound
          and immediately fatal. Every function inside it still ships disabled — see the KDoc.
        -->
        <service
            android:name=".agent.YlihAppFunctionService"
            android:enabled="@bool/enablePlatformAppFunctionService"
            android:exported="true"
            android:permission="android.permission.BIND_APP_FUNCTION_SERVICE"
            tools:targetApi="36">
            <property
                android:name="android.app.appfunctions.schema"
                android:value="app_functions_schema.xsd" />
            <property
                android:name="android.app.appfunctions.v2"
                android:value="ylih_app_functions.xml" />
            <intent-filter>
                <action android:name="android.app.appfunctions.AppFunctionService" />
            </intent-filter>
        </service>

        <property
            android:name="android.app.appfunctions.app_metadata"
            android:resource="@xml/app_metadata" />

        <!--
          ACL_CONNECTED/ACL_DISCONNECTED carry FLAG_RECEIVER_INCLUDE_BACKGROUND, so this manifest
          receiver still runs on Android 8+ with nothing of ours resident.

          exported="true", and it has to be. This used to read "false" with a comment saying the
          system delivers protected broadcasts to non-exported receivers — which was true when it
          was written and is not any more. The Bluetooth stack is a separate app (uid 1002), and an
          implicit broadcast from another app no longer resolves a non-exported component of ours:
          AMS drops it in SaferIntentUtils.filterNonExportedComponents, before the BroadcastRecord
          exists, so it does not even appear as a skipped receiver in `dumpsys activity broadcasts`.

          Measured on a Mi 10T running Android 16: with exported="false" this receiver saw nothing
          across five Bluetooth off/on cycles while another app's *exported* receiver logged every
          one of them; flipping this attribute alone made it fire on the next cycle. That is the
          whole of Bluetooth-only tracking, so with it false the default mode records nothing.

          #34 read that as a confound and reverted it; re-measured on the same phone on Android 17,
          it is not. This time the receiver *lists* were read rather than the app's own state: one
          real reconnect reached the exported BtBatteryReceiver of one install while omitting the
          non-exported BtConnectionReceiver of that same process, in the same second, and a build
          carrying the two flags inverted inverted the verdicts with them. What made #34 look right
          is that detailed tracking was on, and TrackingService.syncWithSystem() reconciles against
          the AudioManager list every minute — it opens exactly the session and heartbeat that were
          taken as proof the receiver had fired. ReceiverExportTest pins the attribute now.

          Exporting costs nothing here: both actions are <protected-broadcast>, so only the system
          may send them and no other app can forge a connection.
        -->
        <receiver
            android:name=".tracking.BtConnectionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>
        </receiver>

        <!--
          The headset's own battery level, for charge cycles. The action is @SystemApi and so cannot
          be named from the SDK, but it is a protected broadcast sent with BLUETOOTH_CONNECT as its
          receiver permission and the same FLAG_RECEIVER_INCLUDE_BACKGROUND the ACL broadcasts
          carry — so it reaches a manifest receiver with nothing of ours resident, exactly as they
          do, and exported for exactly the same reason theirs is. That reason was inherited from the
          ACL case rather than checked here until it was: flipped to exported="false" on one install,
          a real battery broadcast at a headset reconnect listed only the other install's exported
          receiver and omitted this one. tracking/BatteryBroadcast.kt has the whole reasoning.
        -->
        <receiver
            android:name=".tracking.BtBatteryReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.BATTERY_LEVEL_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".tracking.BootReceiver"
            android:exported="false"
            tools:ignore="BatteryLife">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!--
          The three home-screen widgets. Exported because the launcher — another app — is what
          binds and updates them; APPWIDGET_UPDATE is a protected broadcast only the system sends.
          android:label is the name shown in the widget picker.

          Nothing here is flavor-specific: a widget needs no permission the classic build has and
          the play build does not, so these stay out of the classic manifest overlay.
        -->
        <receiver
            android:name=".widget.LifetimeWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_lifetime_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_lifetime_info" />
        </receiver>

        <receiver
            android:name=".widget.ActivityWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_activity_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_activity_info" />
        </receiver>

        <receiver
            android:name=".widget.ChartWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_chart_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_chart_info" />
        </receiver>
    </application>
</manifest>
